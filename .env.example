# CodeMind CLI — environment variables
# Copy to .env for local development. Never commit .env.

# Required for `codemind check --think`, `codemind see`, `codemind trace`
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override log level (default: warn in production, debug in dev)
LOG_LEVEL=debug
