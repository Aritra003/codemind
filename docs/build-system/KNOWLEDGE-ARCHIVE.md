# KNOWLEDGE-ARCHIVE.md — Permanent Institutional Record
# Append-only. No size limit. Never compress. Never delete.
# Contains: graduated working-reference entries + all CRITICAL + all security entries.
#
# LOAD PATTERN (do NOT load at session start — use KNOWLEDGE-BASE.md for that):
#   DOCTOR:       load when debugging unfamiliar symptoms — search ARCHIVE INDEX by domain tag
#   SCHOLAR:      load quarterly for KB compression pass
#   New project:  load to extract project-agnostic patterns; strip project-specific detail
#
# Entry format: identical to KNOWLEDGE-BASE.md entries, with one addition:
#   Graduated: [ISO date]  ← appended to Source line on graduation
#
# Graduation: entries move here from KNOWLEDGE-BASE.md when they are:
#   ≥2 sprints old AND pattern is stable AND not an active/unresolved vulnerability.
#   CRITICAL and security entries graduate after 3 sprints — kept here forever.
================================================================================

## ARCHIVE INDEX
# One line per archived entry — read this to find entries without loading the full file.
# Format: [SEVERITY] [domain] [date] | [one-line lesson summary] | Line: [N]
# Update this index each time a new entry is appended.

================================================================================
## ARCHIVED ENTRIES
# Entries appear here in graduation order (oldest first).
# Each entry is preceded by: ## GRADUATED [ISO date] from KNOWLEDGE-BASE.md
